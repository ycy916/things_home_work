swagger: "2.0"
info:
  title: 띵스플로우 과제 API Docs
  version: 0.0.1
host: localhost:7300
basePath: "/api/v1"
schemes:
- http
- https

tags:
- name: user
  description: 사용자에 관한 api
- name: heart
  description: 하트 관련 api 
  
securityDefinitions:
  Bearer:
    type: apiKey
    name: Authorization
    in: header
paths:
  $ref: './path/index.yaml'
definitions:
  $ref: './path/definition.yaml'


